name: cardscan_example
description: "Demonstrates how to use the cardscan plugin."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  shs_cardscan:
    path: ../

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  analysis_lints: ^1.0.9

flutter:

  uses-material-design: true
